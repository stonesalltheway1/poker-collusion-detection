# LEDGER — Slash poker collusion (append-only)

Format: exp | date | CV total (pairAP / evMAP5 / behMAP) [labelled-only unless noted] | Δ vs parent | LB | decision + mechanism + next

## CV ↔ LB calibration
| exp | CV total | CV pairAP (lab) | CV pairAP (+unknown neg) | LB public | note |
|---|---|---|---|---|---|
| exp004 (sub001) | A .9785 / ev .227 / beh .953 | .9785 | B .579 · C .453 | **0.61704** | generic rates + rule evidence |
| sub002 = exp005 risk + exp004 rule ev routed | C_ap .684 · C_behAP .687 | .9956 | B .711 · C .684 | **0.78984** | ΔLB +.173 vs predicted ≈ +.19 → view C tracks LB. Implied LB pairAP ≈ .93 (if evMAP≈.23, BehAP≈.97·AP) → evidence is the main lever |
| sub003 PROBE = sub002 with NO_EVIDENCE | — | — | — | **0.73452** | ⇒ 0.7·AP+0.1·BehAP = .7345 → LB pairAP ≈ .92 (BehAP≈.97AP). Rule evidence LB contribution .0553 → **eval evMAP5 ≈ .277 vs dev OOF .227** (eval ≈ 1.2× dev, smaller candidate pools). Leader .938 ⇒ their evMAP ≈ .75–.90. PRIORITY: evidence, then pairAP .92→.97 |
| sub004 = exp009 risk + exp004 rule ev | C .680 · C' .925 | .998 | C' +.032 vs exp005 | **0.80414** | ΔLB +.0143 for ΔC' +.032 (same sign; pair-AP LB SE ≈ .02) |
| sub005 = exp009 risk + **exp007 evidence** (learned, no chronology) | dev MAP@5 .568 | — | — | **0.85923** | evidence LB contribution .1247 → eval MAP ≈ .624 (dev .568) |
| sub006 = exp009 risk + **exp008 evidence** (+within-phase chronology) | dev MAP@5 .711 | — | — | **0.88153** (rank 32/254) | eval MAP ≈ .735. **Chronology confirmed on LB: +.0223 final.** dev OOF MAP predicts eval within .03-.06 (eval easier) |
| sub007 = exp016 risk + exp008 ev | C' .9372 | — | — | **0.90310** | ⇒ 0.7AP+0.1BehAP = .7561 → **LB pairAP ≈ .948** (was .92). ΔLB +.0216 for ΔC' +.0121 |
| sub008 = exp011 risk (spy-removed) + exp008 ev | C' .9409 | — | — | **0.90908** | ⇒ LB pairAP ≈ .955. Spy removal confirmed (+.006). ~rank 6-7; #1 .93827 |
| sub009 = seed-bagged cfg-C risk + exp016ev evidence | C' .9483 · dev MAP .7175 | — | — | **0.90563** (−.0035 vs sub008) | **MISTAKE: changed two components at once** → unattributable. Within LB noise (pairAP SE ≈ .02 → .014 on total; evMAP SE ≈ .03 → .006). exp016ev's SP head has dev/eval skew (dev hs = single-model OOF vs eval 5-model mean). **sub008 remains best; revert to exp008 evidence.** RULE: one component per LB test |
| sub010 = exp029_bag (round-3, 5-mean eval scoring) + exp008 ev | C' .9578 | — | — | **0.91018** | +.0011 vs sub008 |
| sub012 PROBE = sub010 with NO_EVIDENCE | — | — | — | **0.77794** | = 0.7·AP + 0.1·BehAP for exp029_bag (EXACT, same risk/behaviour as sub010/011) |
| sub011 = sub010 with exp027ev evidence | dev MAP .7218 | — | — | **0.91117** (best, 6th public) | evidence MAP exp008 = .6612, exp027ev = .6662 (EXACT) |
| sub013 = exp035_bagsf (same-fold eval scoring, round 4) + exp008 ev | C' .9574 | — | — | **0.91100** | +.0008 vs its matched sub010 → same-fold fix is directionally right, within noise |
| sub016 = exp035_bagsf + exp036xexp027ev | C' .9574 · dev MAP .7710 | — | — | **0.91820 (BEST, 9th)** | the listing rule on LB |
| sub019 = exp038_f1 risk + exp037 evidence | C' .9600 · dev-neutral gate | — | — | **0.91706** (−.0011 vs sub016) | Confounded test disentangled by sub020 (-0.00044) and sub021 (-0.00065) |
| sub020 = exp038_f1 risk + exp036xexp027ev | C' .9600 · dev MAP .7710 | — | — | **0.91776** (−.00044 vs sub016) | Pure risk delta: exp035_bagsf outperforms exp038_f1 on LB (+0.00044) |
| sub021 = exp035_bagsf + exp037 evidence | C' .9574 · dev MAP .7719 | — | — | **0.91755** (−.00065 vs sub016) | Pure evidence delta: hard 0.60 gate hurts LB (-0.00065) due to low-exposure truncation |
| sub023 = exp035_bagsf + exp042_ramp evidence | C' .9574 · dev MAP .7748 | — | — | **0.91845** (+.00025 vs sub016) | Smooth ramp [0.45, 0.65] gate recovers low-exposure pairs; new LB personal best |
| sub024 = exp044_fuse_lgb_xgb risk + exp042_ramp evidence | C' .9585 · P(better) .848 | — | — | **0.91954** (+.00109 vs sub023) | Fused LightGBM + GPU XGBoost pair risk model. Major breakthrough past 0.919! |
| sub025 = exp048_fuse_bags risk + exp042_ramp evidence | C' .9586 · P(better) .858 | — | — | **0.91942** (+.00097 vs sub023) | Full 6-model bag (3xLGBM + 3xXGBoost); confirms stable lift of XGBoost diversity |
| sub026 = exp048_fuse_bags + exp027ev (pure learned evidence) | — | — | — | **0.91169** | matched-risk evidence probe: with evidence(exp027ev) = .66615 this pins 0.7·pairAP + 0.1·behAP = **.77846** for exp048, hence evidence(exp042_ramp) = .70480 |
| sub027 = exp048_fuse_bags + exp036 FLAT (w=.7, no gate) | dev MAP .7710 | — | — | **0.91918** | pure evidence delta vs sub025: the smooth ramp is worth **+0.00024**, an independent replication of the +0.00025 measured at exp035_bagsf risk (sub016 vs sub023). Flat evidence = .70360 |
| sub014 PROBE = sub011 with behaviour=other_coordination for all | — | — | — | **0.81453** | = 0.7·AP + 0.2·MAP ⇒ **EXACT: pairAP .9733 · evidence MAP .6662 · BehAP .9664.** Loss budget: pair .019 / **evidence .067** / behaviour .003. #1 .938 ⇒ their evidence ≈ .77-.80 |

## Experiments
| exp | date | CV | Δ | LB | verdict |
|---|---|---|---|---|---|
| exp000 | 2026-09-15 | scaffold: cache, metric replica (perfect=1.0, random=0.140), frozen table folds | — | — | infra |
| recon | 2026-09-15 | Day-0 research (16 agents): family pair AUC dev CI 1.000 (trash-raise rate) / DT 0.994 (pfcall_weak_max) / SP 0.991 (top5 action-LLR); generic PU LGBM labelled-only AP 0.979, pool AP 0.577; evidence proxies MAP@5 DT 0.55 / SP 0.56–0.64 / CI 0.46–0.69 | — | — | Collusion phase-local (0 players active in both phases); hard negs = random clean sample; seat/metadata ≈ AUC 0.5; eval positives est. ~490. Plan: research/SYNTHESIS.md → PLAN.md |
| exp004 | 2026-09-15 | lab-only CV **0.8256** (pairAP_A 0.9785 / evMAP5 0.2270 / behMAP 0.9528 [DT .961 SP .982 CI .916]) · view B pool AP **0.579** (372/137,739) · view C w2000 pool AP **0.453** (lab-only 0.951) | — | pending (sub001) | BASELINE. 110 rate feats from pair_generic (counts/shared, flows per 100h in bb, max/min/absdiff of _12/_21, shrunk conditional rates, log shared); PU LGBM (372 pos + 1488 neg w1 + 20k unknown dev pairs shared≥57 no-pos-player, 50/table, w0.3), 500 fixed rounds, table folds. Top gain: aggr_lift_max (aggr vs partner − aggr when partner out), both_vpip rate, aggr_v rate. Behavior LGBM multiclass on positives, OOF acc 0.979; eval argmax dist SP 60k / CI 36k / DT 16k. Risk = rank(LGBM raw) tie-broken by PU LogReg then feature z-sum → 0 ties. Evidence = spec rule scores (no chronology, tie-break pot bb desc — beats asc on dev: SP .348 vs .325, CI .120 vs .083): dev MAP@5 oracle-routed DT 0.191 / SP 0.348 / CI 0.119 — far below learned-ranker proxies (.52/.56/.46): DT top-bucket (min transfer ≥30bb) precision only 0.20 vs 0.36 at 7–8bb+fold; CI weak-raise flag (top_pct_vs1>0.70) not discriminative (score 9 = partner resp + 4 third folds w/o weak raise has precision 0.54 > score 19). Composite ≈ 0.7·0.453+0.2·0.227+0.1·beh ≈ 0.41–0.46. Next: exp007 in-pair evidence rankers (biggest cheap lever), family pair features. Runtime ~90 s total. |
| exp001 | 2026-09-15 | pair-hand engine (src/engine.py, numba, all 15 seat pairs/hand, 90 feats): dev 18M rows 73s, eval 12M rows 50s. Acceptance vs forensics (evidence / neg-hand rate): CI trash .693/.0039, CI UTG-weak .626/.0117, SP G0 1.000/.219, DT flow 1.000/.156, DT strong .623/.0114, DT gift5 .788/.0187, pop-p_call<=.15 weak call .477/.0022 | — | — | ACCEPT: reproduces forensics within 0.3 pt; policy columns empty until exp003 lands (re-run engine then) |
| exp005 | 2026-09-15 | engine pair features (394 cols: rates, binomial z vs population, own-baseline excess/lift/mutual-top) + PU LGBM (W_U .1, 600 rds): A .9956 · B .711 · **C .684** (DT .504 / SP .415 / CI .443 per-family pool AP) · C_behAP .687 · beh acc .995 | C +0.231 vs exp004 | **LB .78984** | top gain = own-baseline log-lift feats (x_trash, sp_g1, both_vol, strongcheck, pcall05). risk_raw>.5 on 406 eval pairs (~est. 490 positives) |
| exp010 | 2026-09-15 | exp005 but trained on dev 2000-hand windows a+b (exposure-matched): A .9985 · B .715 · C .663 · C_behAP .662 | C −0.021 vs exp005 | — | REJECT (more U weight at n≥38 + duplicated positives; revisit with W_U .05) |
| exp003 | 2026-09-15 | label-free normal-play policy (src/policy.py, 66 public+own-card feats, LGBM 4-class, 2-half table cross-fit, positive-pair hands excluded from training) on all 18.6M actions → data/derived/action_policy.parquet. Held-out logloss/acc: pre .545/.771 · flop .574/.736 · turn .608/.716 · river .580/.733 (marginal baseline .844/.744/.818/.814); p_call calibration within ±.005 up to .6. pfcall_weak_max@.15 DT vs NEG AUC .9930 AP .947 (rate: .9933/.955; @.10 rate .9949/.968); DT vs SP+CI .989. SP passive partner responses mean p_agg .196 vs NEG .059, hand max_miss AUC .959 | — (forensics subset policy .9943) | — | ACCEPT as infra: meets ≥.99 target, phase-stable (dev/eval logloss equal), 8 min end-to-end. Mechanism: population policy makes "what a normal player does here" explicit → DT flat-call and SP passed-raise signatures read directly off p_*. Next: feed surprise/p_* aggregates into exp005 pair features + evidence rankers; variant with OOF player-style baselines. Doc research/POLICY.md |
| viewC' | 2026-09-15 | NEW PROXY: view C with frozen spy list removed (128 unlabelled dev pairs with exp005 OOF>.3 = likely hidden positives; data/derived/spies_exp005.parquet; scripts/eval_views.py). C is capped by hidden positives and PENALISES models that find them. C' exp005 .893 · exp005 W_U .03 .897 / .3 .892 · exp010 (w2000ab) .922 · exp010_all3 .923 | — | — | Decide pair-model changes on C' (biased toward exp005 if anything). exp010 verdict REVERSED → accept exposure-matched training |
| exp009 | 2026-09-15 | engine re-run with exp003 policy (ph_v2, pfv2: +policy flags polcall15/05, polagg30, polfold20/05, flow-gated polfold) + all3-window training W_U .1: C .680 · **C' .925** (DT .913 / SP .809 / CI .955) · C_behAP .686 · beh acc .992 | C' +.032 vs exp005 | **LB .80414** (+.0143 vs sub002) | policy adds ~+.002 over exp010_all3; SP weakest family → next: hand-level planted detectors + top-k aggregation (exp015) |
| exp007 | 2026-09-15 | **evidence OOF MAP@5 (true-family routing) DT 0.6000 / SP 0.5888 / CI 0.4860, host all-372 0.56786** (uniform posteriors 0.5932/0.5697/0.3525 = 0.52537) | vs exp004 rule evidence (0.191/0.348/0.119 = 0.227): **+0.34 MAP = +0.068 final** | pending (sub002) | ACCEPT. `src/evidence.py` (stages cache|cv|eval|compose, ~28 min, 4 threads, <3 GB) -- in-pair evidence rankers on the exp001 engine (ph_v2, incl. exp003 policy cols; EVID_PH selects the dir). 321 orientation-free hand features: max/min + hand-donor + pair-donor orientation of all 36 directional engine feats, family gates (DT flow ev-cov 1.000 / strong .623 / gift5 / pfweak, SP G0 1.000 / G1 / S_PL / strongcheck, CI S1 .693 / S14 .626 / S15 .902 / S7, union gate 1.000 on all evidence), within-pair percentile + top-rank of 19 key scores, orderless pair context (counts/rates/log shared). LightGBM per-family vs pooled(one-hot) x binary vs lambdarank x rounds grid, OOF by frozen table folds, target = listed evidence among the pair's own hands. Chosen: DT stacked fam-binary 100, SP stacked fam-binary 200, CI single pooled-binary 200. Config spread ~0.01 = noise (per-family SE .025-.03) so the selection max is optimistic by ~that. Beats every no-chronology forensics recipe (DT .525 / SP .558 / CI .464). Scores = Platt-calibrated P(listed) so the family mixture is the Bayes marginal; percentile normalisation is worse (uniform .514 vs .525). exp003 policy cols: +.001 DT / +.003 SP / +.019 CI (policy-less run kept in evidence_cache/ph_nopolicy/). Files: data/derived/evidence_scores_{dev,eval}_exp007.parquet (eval = per-family top-15 union, 2.04M rows, all 112,540 pairs, >=15 candidates each) + compose_evidence() in src/evidence.py; composed eval evidence passes validate_submission (non_eval_phase 0, not_shared 0, 5 distinct hands/pair). Next: feed real exp006 posteriors -- routing is now the binding constraint (uniform .525 vs true-family .568). Doc: research/EVIDENCE.md. |
| exp008 | 2026-09-15 | **evidence OOF MAP@5 (true-family) DT 0.7164 / SP 0.7078 / CI 0.7066, host all-372 0.71091** (uniform 0.6627/0.6304/0.5661 = 0.62734) | vs exp007 **+0.116 DT / +0.119 SP / +0.221 CI = +0.143 MAP = +0.029 final** | pending (sub003 A/B) | ACCEPT-but-GRAY. exp007 + within-phase chronology (nested two-stage): q = relative position in the pair's phase timeline, prior counts/shares of earlier gate and SUB-TYPE hands (Critic C3: DT fold-to-partner vs call-down, SP fold vs call-down, CI UTG weak open), plus stage-1-model chronology (K = summed score of earlier hands = learned p*exp(-K/2), # earlier hands >.1/.25/.5, # earlier hands inside the pair's stage-1 top-5/10, same-sub-type versions). Nested cross-fit: stage-1 scores for stage-2 TRAINING rows come from inner fits that exclude the outer fold, so no fold-k label reaches fold-k features. **Decisive ablation: ORDERLESS stacking (exp007s = stage-1 score + within-pair rank/pct/threshold counts, no order) = DT .600 / SP .589 / CI .471, i.e. ~0 over exp007 -- the entire gain is chronology, not the second stage.** Beats the forensics chronology recipes (DT .554 -> .716, SP .638 -> .708, CI .686 -> .707); their p*exp(-K/2) on our stage-1 scores = .680 on DT. MECHANISM/CAVEAT: dev lists are ~'the first 5 planted hands of the phase, in two sub-type segments'; whether the private eval lists were sampled the same way is unknowable locally -> LB A/B, sub002 (exp007 evidence) vs sub003 (exp008 evidence) with the same risk+behaviour; expected delta +0.029 if the rule carries over, ~0 if not. Uses only gameplay order inside the eval phase (never row/file order or ID structure) but it is the PLAN.md gray item -> disclose in the prize write-up if used. Files: evidence_scores_{dev,eval}_exp008.parquet (eval 2.28M rows). Doc: research/EVIDENCE.md. |
| exp013 | 2026-09-16 | other_coordination hunt (scripts/oc_hunt.py: 62 directed label-free events x 3 windows, own-baseline row-x-col LLR + mutual-top, binomial/negative/other-phase nulls) — **NULL**: no 4th mechanism. Eval top-450 is 96% explained by high-precision family flags (25 unexplained, all soft-play-profile); symmetric dev-vs-eval McNemar on 37,098 exposure-matched pairs flat (\|z\|<=1.5) so no eval-only planting; information-sharing/ghosting/best-hand-plays/blind-steal/co-presence/metadata all exactly null; one residual (`third_folds_pair` +137 dev tail, +8 eval) = position pairing + weak DT/CI hidden positives, hands read as normal poker. LB arithmetic bounds the missed share: implied pairAP .93 => phi <= .07 | — | — | STOP hunting OC. Byproduct kept: `data/derived/oc_pair_scores_{dev_full,eval}.parquet` (oc_score = mutual-top sum-G, 26 event G/mtop cols, family flags) — label-free, labelled-only AP .867 / AUC .951 (DT .977 SP .951 CI .910), pool AP .306, rank-corr with exp005 on unknown pairs only 0.21 => feed to exp014 generic branch (LOFO-validated). `oc_hand_candidates_eval.parquet` = top-5 generic hands for the 496 family-less eval pairs in top-1200 (evidence padding). Behaviour: do NOT add an other_coordination class (unexplained rate inside eval top-450 is 5% < the dev positives' own 9.7%). Notes: research/forensics/other_coordination.md |
| exp013b | 2026-09-16 | exp013 OC pair stats exported for the two exposure-matched dev windows (`oc_pair_scores_dev_w2000a/b.parquet`, 174,000 pairs each, identical schema to dev_full; base rates/shrinkage/own-baseline ranks/family thresholds recomputed within each window). Median shared n 62/61 vs eval 61; oc_score p50/p99 3.5/86 in all three 2000-hand windows (dev_full 3.9/84). Labelled-only AUC(oc_score) .951 dev_full / .907 w2000a / .920 w2000b (exposure only) | — | — | feature block now exists for all four pair-model windows (exp005 TRAIN=all3 + eval) |
| exp007/exp008 | 2026-09-16 | evidence rankers (src/evidence.py; per-family LGBM, in-pair target = listed evidence, Platt-calibrated, nested chrono stage-2): exp007 dev OOF MAP@5 true-family DT .600 / SP .589 / CI .486 → .568 (uniform .525); exp008 +chronology DT .716 / SP .708 / CI .707 → .711 (uniform .627). Orderless-stage1 ablation ≈ exp007 → the whole exp008 lift IS chronology | evMAP +.341 (exp007) / +.484 (exp008) vs exp004 rules | LB .85923 / .88153 | ACCEPT both; chronology kept pending user's final call. Routing is now binding (uniform vs true-family gap .04-.08) |
| exp013 | 2026-09-16 | other_coordination hunt (scripts/oc_hunt.py, 65 label-free directed events, 3 nulls incl. phase-contrast + McNemar): **documented NULL** — no unexplained excess; instrument recovers DT .68-.74 / SP .30-.61 / CI .40 recall at 0/1488 neg FPR. OC share bounded ≤7-10% (LB arithmetic + 96% of exp005 top-450 eval pairs carry a known-family signature) | — | — | STOP hunting OC. Its oc_score is a genuinely new feature (rank corr .21 with exp005; labelled-only AP .867) → folded into exp016. Do NOT add an other_coordination behaviour class (would delete more true family pairs than it fixes) |
| exp015 | 2026-09-16 | hand-level planted detectors (src/handdet.py, LGBM 4-class on evidence hands vs negative-pair hands, OOF by table): AUC vs neg DT .996 / SP .993 / CI .993, any .989. Pair aggregation = top1/3/5/10, mean, threshold counts+z, double-centred top5 | — | — | ACCEPT (feeds exp016) |
| exp016 | 2026-09-16 | exp009 + handdet top-k + exp013 OC features, all3 training: C .679 · **C' .9372** (DT .916 / SP .860 / CI .952) · C_behAP .682 | C' +.0121 vs exp009 | sub007 pending | top gain h_s_any_mean/top3/top5_dcmin + q999 counts, then fam_unexplained, G_limp_first, oc_score. SP gap closed .809→.860 |
| exp011 | 2026-09-16 | exp016 + drop the 128 frozen spies from the training negatives: C .6974 · **C' .9409** (DT .918 / SP .868 / CI .942) | C' +.0037 | **LB .90908** | ACCEPT (hidden positives trained as negatives were suppressing the signal) |
| exp017 | 2026-09-16 | exp011 + within-table z of the top-25 features: C' .9410 | +.0001 | — | REJECT (noise) |
| exp016ev | 2026-09-15 | **evidence v2 dev OOF MAP@5 (true-family) DT 0.7261 / SP 0.7156 / CI 0.7066 = 0.7175; with REAL exp016 posteriors 0.7150** (exp008: 0.7164/0.7078/0.7066 = 0.7109, posteriors 0.7068) | **+0.0066 true-family / +0.0082 posterior-routed MAP = +0.0013..0.0016 final -- BELOW the 0.02 noise bar**, but a consistent shift across all rounds/objectives, not a grid max | not submitted (exp008 = LB 0.88153 stays the fallback) | src/evidence_v2.py. (a) SUB-TYPE CHRONOLOGY (Critic C1/C3) now measured: segment labels recovered from the single list inversion (DT 91/148, SP 93/132+3, CI 11/92; 949 hands), segment-1-vs-2 classifier OOF AUC DT .959 / SP .946 / CI .866 -> as ranker features +0.010 DT, +0.001..4 SP, -0.005 CI. (b) exp015 hand-detector scores (hs/): +0.008 SP, -0.013 CI, neutral DT; dev hs is single-model OOF vs eval 5-model mean, so SP carries a mild shift risk (DT/CI picks are hs-free). (c) ROUTING IS SOLVED: soft exp016 posteriors cost only .003-.004 vs oracle routing (0.7150 vs 0.7175); sharpening T=2/4 and argmax are inside noise; the exp013 fam_unexplained->SP override HURTS (-0.002; only 36/372 dev positives carry the flag and behaviour already routes them). CLOSED LANES: explicit two-segment quota composition is far worse at every n1 (best DT .618 vs flat .726 -- the segment classifier labels ~90% of the 100+-hand pool 'segment 2', so quotas discard good candidates); a listwise shortlist RE-RANKER over the top-8/10/12/16 (binary + lambdarank trunc-5, 50-400 rounds, shortlist-structure features) is below flat for every family (-0.001..-0.006) despite the diagnostic that perfect ORDERING of our own top-5 would give 0.7716 (+0.054) and 96% of listed hands already sit in our top-10 (AP@10 0.831 / AP@20 0.847) -> the residual is the host's listing choice among near-identical planted hands, not detection. Files: evidence_scores_{dev,eval}_exp016ev.parquet (eval 2.36M rows, all 112,540 pairs; composed evidence passes validate_submission; shares 4.14/5 hands per pair with exp008), evidence_cache/cv_results_v2.json. Runtime CV 1556 s + eval 415 s, 4 threads, <4.4 GB. Doc: research/EVIDENCE.md 8. |
| exp018/exp019/exp020 | 2026-09-16 | pair-model configs on C' (paired table-bootstrap, 400 reps): cfg B .9434 (P(better) .748) · **cfg C goss/63-leaf .9477 (P .990, +.0064)** · rank-fusion A+B+C .9461 (P .995) · fusion B+C .9471 (P .980) · C vs fusion-BC tied (P .675) · seed-bag 3x cfg C .9483 (P .777) | C' +.0074 vs exp011 | (in sub009, confounded) | ACCEPT cfg C + seed bag (variance); fusion adds nothing over C alone |
| exp021 | 2026-09-16 | + eval-window rows of labelled pairs as clean negatives (1430 rows; phase-locality makes them genuinely non-coordinated): C' .9454 | −.0023, P(better) .013 | — | REJECT |
| exp022 | 2026-09-16 | + statistical aggregators of hand scores (sum of clipped logits, positive-part sum, top3 sum, 2nd max, Fisher-z of exceedance p-values): C' .9484. sumposlogit becomes the top feature by gain | +.0007, P .675 | — | keep (harmless, top feature) but NOT a real gain |
| diagnosis | 2026-09-16 | WHY pairAP is stuck ≈.955: of 328 dev positives 314 rank in the top 600 and only 31 negatives outrank the 300th positive. The stragglers are LOW-EXPOSURE (median 57 shared hands vs 117, 3 evidence hands in window vs 5). Their planted hands ARE detected (median max s_any .952, max over evidence hands .910) but top5-style features dilute → this is an exposure/aggregation problem, not detection | — | — | → self-training to raise per-hand recall (exp024) |
| exp024 | 2026-09-16 | self-trained hand detector: non-listed hands of positive pairs with round-1 s_family>.5 added as weak positives (w=8) — forensics says unlisted planted hands ≈2× listed. Hand OOF AUC vs neg DT .9963→.9983 · SP .9931→.9957 · CI .9926→.9964 · any .9894→.9945; recall@q999 DT .65→.82 / SP .41→.59 / CI .62→.83 (NB: metrics include the self-labelled positives → optimistic; real test is pair C' + evidence MAP) | pending | — | rescoring into hs_v2 |
| exp023ev | 2026-09-15 | **NULL (quantified): calibrated segment-quota composition does not beat the flat ranker.** Dev OOF MAP@5 true-family, baseline exp016ev flat DT .7261 / SP .7156 / CI .7066 vs best v3 policy DT .6940 / SP .7036 / CI .6834 | **-0.012 .. -0.032** (every policy loses) | not submitted; no new score files | src/evidence_v3.py. The v2 quota failure was NOT (only) calibration: rebuilt as a 3-CLASS cross-fit model (0 not listed / 1 listed-seg1 / 2 listed-seg2) and as a FACTORED model P(seg_s|h)=P(listed|h)*P(seg2|listed,h). Calibration is now correct (factored listed mass 4.90 = true 4.90; mean k1_hat 2.71 vs true 2.59 DT; corr(k1_hat,k1_true) .51 DT / .35 SP / .26 CI) and the flat top-5 ALREADY has the right segment mix (DT 2.10 seg2 picks vs true k2 2.34; SP 2.86 vs 2.50). Policies tested: flat, fixed quota k1=1..4, per-pair estimated k1, greedy decision-theoretic E[AP@5] with segment-slot discounting, each with chronological (Critic C1) and probability ordering -- all below flat; chronological-within-segment ordering is 2-4 pts worse than probability order. **ORACLE BOUND: with the TRUE k1 the quota DOES win (+0.0149 DT / +0.0088 SP / +0.0109 CI on the one-inversion pairs), i.e. the structure is real but the whole lane's ceiling is ~+0.008 MAP = +0.0016 final over all 372 positives -- under the noise bar even with a perfect k1 predictor.** Under independence, ranking by P(listed) is provably E[AP@5]-optimal in set and order, so only the within-segment dependence was ever available, and the ranker's segment-chronology features (exp016ev C_seg, +0.010 DT) already capture most of it. LANE CLOSED: three attacks on the selection gap have now failed (v2 pool quotas, v2 shortlist re-ranker, v3 calibrated quotas/greedy). Reopen only with a k1 predictor at corr > 0.8 or segment truth for the zero-inversion pairs (DT 57 / SP 39 / CI 81). Doc: research/EVIDENCE.md 9. Runtime 81 s total. |
| exp023ev | 2026-09-16 | evidence segment-composition retry with calibrated 3-class/factored segment model (agent): k1 estimate now unbiased (mean 2.71 vs true 2.59, corr .51 DT) but flat top-5 ALREADY picks the right segment mix (DT 2.10 seg-2 vs true 2.34). Oracle-k1 quota ceiling +.0149 DT / +.009 SP / +.011 CI on the 61%/70%/12% of pairs with the structure ⇒ **whole lane ≤ +.008 MAP = +.0016 final**. Theory: ranking by P(listed) is E[AP@5]-optimal under independence | — | — | **NULL #3 — lane CLOSED** (with pool-wide quotas and shortlist re-ranking). Reopen only with a k1 predictor at corr>.8 |
| exp024 | 2026-09-16 | self-trained hand detector → pair model (cfg C, all3): C .7224 · **C' .9531** · C2 .9607 | +.0045, **P(better) .978** | — | ACCEPT — best single gain since exp011. Mechanism: unlisted planted hands double the hand-level positive set → better recall on low-exposure pairs |
| exp025 | 2026-09-16 | + explicit mixture-LLR pair features (scripts/mixllr.py; sum_h log(1+pi(LR(s_h)-1)), calibrated f1/f0 bins): C' .9533. STANDALONE the statistic ranks at only .78 AP vs the model's .948 → hands within a pair are far from independent and the learned aggregation already beats the idealised statistic | +.0002 | — | keep as features (harmless), not a real gain |
| exp026 | 2026-09-16 | + pair-level self-training: the 154 frozen likely-hidden-positives promoted to positives (w .5), scored against ORIGINAL labels (y_orig): **C' .9556** · C2 .9624 (DT .9498) | +.0023, P(better) .807 | — | marginal ACCEPT (doctrine P>=.8) |
| calibration FIX | 2026-09-16 | Earlier "eval evMAP ≈ .277/.735" used a probe with a DIFFERENT risk model. Matched arithmetic: sub004 (exp009 risk + rule ev) − 0.2·MAP(rule .2766) ⇒ 0.7AP+0.1BehAP = .74882 for exp009 ⇒ **eval MAP(exp008) ≈ .664** (not .735) and **AP(exp009) ≈ .941** (Beh ≈ .95AP). So eval evidence is HARDER than dev OOF (.707), consistent with ~6-7% of true positives being other_coordination whose evidence our family rankers never retrieve (evidence MAP averages over ALL true positives) | — | — | Next LB day: 2 probes to pin AP/MAP/BehAP exactly on the CURRENT pipeline; plus an OC-evidence swap test |
| BUGFIX | 2026-09-16 | scripts/fuse.py dropped y_orig and scripts/boot.py ignored it → spy-promoted runs were being scored against their PROMOTED labels (inflated: exp027_bag C read .9668 vs .9561 honest). Both fixed; all numbers below are on ORIGINAL labels | — | — | Lesson: any relabelling experiment must carry y_orig through every downstream scorer |
| exp027 | 2026-09-16 | seed-bag (3x) of exp026 (cfg C + self-trained hand scores + mixllr + spy-positives): **C' .9561** · C2 .9627 (DT .9508 / SP .8845 / CI .9765) | +.0028 vs exp025, P(better) .843 | — | BEST pair model. Cumulative C' .9484 → .9561 today |
| infra | 2026-09-16 | src/run_all.py = end-to-end reproduction (12 stages, skip-if-exists, env-pinned); scripts/case_review.py renders submitted evidence as readable hand histories + benign alternatives (prize requirement + qualitative check — top case reads as genuine soft play: partners check to each other every street, then A calls B's 74.5bb raise with two pair into B's made straight, -92bb) | — | — | final.csv built from exp027_bag + exp008 evidence, ready for the 00:00 UTC window |
| exp027ev | 2026-09-15 | **evidence on hs_v2: dev OOF MAP@5 DT 0.7297 / SP 0.7236 / CI 0.7066 = 0.7218 nested; 0.7222 / 0.7191 / 0.6907 = 0.7118 under the shipped-model protocol (real posteriors 0.7115)** | vs exp016ev under the SAME protocol: **+0.0043 (nested) / +0.0086 (shipped)** -- consistent in sign, **below the +0.02 bar** | files written, not submitted | src/evidence_v4.py. (a) The better detector helps only SP/DT: stage-1 MAP with hs_v2 DT .598->.576, SP .584->.595, CI .486->.439; like-for-like hs_v1->hs_v2 inside D_hs_seg is SP +.012 / DT +.003 / CI -.005. Mechanism: self-training makes the detector fire on the ~2x more numerous UNLISTED planted hands, which is the wrong axis for in-pair LISTING. (b) 'hs as the sole hand-quality block' fails hard (stage-1 DT .491 / SP .524 / CI .259; stage-2 .601/.587/.438) -- the engine features carry the listing signal. Chosen: DT D2_hsv2_seg|lambdarank|100, SP D2_hsv2_seg|binary|200, CI A_base|lambdarank|350 (unchanged). (c) **LOFO (OC proxy): a family-agnostic planted-ness head is WORSE than the current posterior mixture on 2 of 3 held-out families (DT .359 vs .440, CI .060 vs .183, SP .442 vs .425) -> do NOT add an OC fallback head.** (d) **The dev->eval evidence gap is now fully accounted for: OC share ~6.5% x (0.72 - ~0.35 LOFO) = -0.025, plus -0.014 of shipped-vs-nested protocol optimism => 0.707 - 0.040 = 0.667 vs the measured eval 0.664.** (e) CAUTION: the same config scores 0.703-0.722 depending on how the final stage-2 is built/measured (+-0.014), wider than the gain being chased; the dev files for exp008/exp016ev (nested protocol) overstate their shipped models by ~that much. (f) hs_v2 dev/eval skew on like-for-like populations is only 1.2-1.4x in the tail, so raw hs_v2 is safe; rank-only variant available at -0.004 DT / -0.010 SP. Files: evidence_scores_{dev,eval}_exp027ev.parquet (eval 2.22M rows, 112,540 pairs, passes validate_submission, shares 4.55/5 hands per pair with exp016ev). Runtime stage1 335 s + cv 1436 s + lofo 91 s + eval 281 s. Doc: research/EVIDENCE.md 10. |
| exp029/exp030 | 2026-09-16 | self-training round 3 seed-bag: C' .9578 (tied with the single model .9581, P .772 vs exp027_bag). **Round 4: C' .9582, +.0001, P(better) .525 → self-training has CONVERGED at round 3** | — | — | PAIR MODEL FROZEN at exp029_bag (bag, for private-LB stability). Cumulative today C' .9484 → .9581 |
| exp027ev | 2026-09-16 | evidence rankers rebuilt on the self-trained hand scores (agent): hs_v2 helps SP (+.012) / hurts CI (−.005) / DT ~0 — better *detection* is the wrong axis for in-pair *listing*. exp027ev vs exp016ev +.004 (nested) / +.009 (shipped protocol). LOFO: a family-agnostic head is WORSE than the posterior mixture (DT .359 vs .440, CI .060 vs .183) | +.009 dev MAP | — | ACCEPT as an A/B candidate only. **Evidence gap fully explained: OC ~6.5% at ~.35 → −.025, protocol optimism −.014 ⇒ .667 predicted vs .664 measured.** CAUTION: protocol sensitivity ±.014 is wider than the gain |
| staged | 2026-09-16 | Ready for the 00:00 UTC window, clean one-component-at-a-time design: **sub010** exp029_bag + exp008 ev (vs sub008 = pure pair-model delta) · **sub011** exp029_bag + exp027ev (vs sub010 = pure evidence delta; 41% of evidence cells differ) · **sub012** probe = sub010 with NO_EVIDENCE (exact MAP for this risk) | — | — | top-500 family mix stable (DT 190 / SP 155 / CI 155) |
| exp031 | 2026-09-16 | TRANSDUCTIVE self-training: top-350 target-window pairs as pseudo-positives (precision 260/350 = 74%) + target rows in training, refit. Dev-validated procedure: **C' .9438 vs .9581, P(better) .000** | −.0134 | — | **REJECT / NULL.** 26% wrong pseudo-labels poison the fit; unlike hand-level self-training (where the extra positives are genuinely planted hands), pair-level pseudo-labels invent positives. Lane closed |
| exp032 | 2026-09-16 | **learned SEQUENCE model over the ordered action stream** (src/seqmodel.py: 34-token transformer d96x3L over per-action tokens [role A/B/other x action x street + 28 continuous: bb-scaled amounts, policy p_*/surprise, omniscient eq_pre/eq_post/delta for A and B, facing-A/B, A-vs-B hu equity] + 46 static; A/B swap = a fixed column permutation + role remap, verified exactly, random-swap training + both-orientation inference; cross-fit on the frozen table folds; 223,749 rows / 204,325 hands / 1.90M tokens cached in 37 s; 942 MB VRAM). **(1) HAND DETECTOR** on handdet.py's exact SELFTRAIN population (182,801 rows): seq OOF AUC vs clean-pair hands DT .99773 / SP .99356 / CI .99644 / any .99378 vs the LightGBM re-fit on the SAME rows+folds .99854/.99596/.99712/.99516 (which itself reproduces shipped hs_v4 to 1e-4) -> **seq LOSES on every head**; rank corr seq-vs-LGBM only .72-.76. Blends move AUC +.0001..+.0004 (noise) BUT on the operating point the pair model actually consumes (recall of LISTED evidence hands at the negative q999 threshold) LGBM+seq-margins-as-features gives DT .6579->.6731 / **SP .4098->.4810** / CI .6457->.6478, paired McNemar z 1.68 / **4.67** / 0.33. **(2) EVIDENCE RANKER** on the 45,129 dev positive-pair rows (aligned 1:1 with dev_pos; harness reproduces the exp008 dev file's .7164/.7078/.7066 exactly): seq alone, chronology-free, MAP@5 .5304/.5406/**.4886** vs engine stage-1 same protocol .5913/.5748/.4661 -> below on DT/SP, **above on CI**. Nested exp008 two-stage, paired, +/- the 3 seq margins: DT .7187->.7122, SP .7065->.7011, **CI .6837->.7187 (+.0350)**; CI-only confirm in both stage-2 objectives: binary +.0350, lambdarank .6897->.7214 (+.0317), all 6 cells positive. Post-hoc rank average with the SHIPPED exp008 dev scores (w .15): DT +.0013 / SP -.0105 / **CI .7066->.7179** | seq alone: -.0014 AUC any, -.061 DT / -.034 SP MAP; blends: +.071 SP listed-hand recall (z 4.67), +.032..+.035 CI evidence MAP paired (+.012..+.016 vs the published .7066) = **+.0035 overall MAP = +.0007 final** | not submitted; no score files written | **MOSTLY NULL as a replacement (the mechanism is measured, not assumed: 1,817 positives is too few for a transformer to beat engine.py), TWO REAL-BUT-SUB-NOISE LEADS as an added member.** CI evidence is the first movement on that number all campaign (.7066 identical in exp008/exp016ev/exp027ev). NOT SHIPPED: cashing either lead needs eval-phase seq scores = a cache over ~800k eval hands (6.9M evidence rows / 12M pair-hand rows), refitting+persisting the fold models (not saved), and a rerun of evidence_v4 eval and/or handdet score -> pairfeat -> pair model = 2-4 h for +.0007 expected, at D-4 with the pair model frozen and sub010/sub011 already staged one-component-at-a-time. CAVEATS: my base CI reproduction (.684-.694) is .012-.023 BELOW the published .7066 so part of the +.035 is recovering my own harness deficit; the seq scores fed to the GBDT are cross-fit but NOT nested (same protocol as the shipped hs features; exp027ev measured ~.014 of optimism in exactly this comparison). REOPEN IF: CI delta >= +.03 on a 2nd seed AND under nested seq scores (-> swap the CI head only as a single-component A/B); or a pair-model refit shows the SP recall gain >= +.002 C' at P>=.8. **Highest-value untried follow-up: pretrain the trunk on the 183k-row detector task, fine-tune on the 45k-row listing task (identical architecture, only the head differs, ~30 min GPU) -- the data-poverty fix for lane (2).** Doc: research/SEQMODEL.md |
| exp032b | 2026-09-16 | exp032 follow-ups (same session): **seed stability + seed bag + a pretrained trunk.** Seq-alone listing MAP@5 is seed-stable (s0 .5304/.5406/.4886 · s1 .5194/.5445/.4860) and **pretraining the trunk on the 183k-row detector task then fine-tuning (12 ep, lr 7e-4) changes nothing (.5302/.5453/.4824)** -> the data-poverty hypothesis for the listing lane is FALSIFIED (a planted-ness prior is the wrong axis for listing, same mechanism as exp027ev). CI nested two-stage with vs without the seq margins (row mean r100/200/350; paired bootstrap over the 92 CI pairs): s0 binary .6837->.7187 / lambdarank .6897->.7214; **s1 .6837->.7296 / .6897->.7346**; bag(s0+s1) .7262 / .7256; **pretrained+fine-tuned .6837->.7343 (P 1.00/1.00/1.00)** -> **21/21 cells positive, every P(better) >= .978**. All-family bag: DT -.0044 / SP +.0020 / CI +.0425 -> seq column belongs in the CI head ONLY | CI evidence +.032..+.051 paired, **+.012..+.028 vs the shipped .7066** => ~+.005 overall MAP => **~+.001 final** | not submitted; no score files | **LEAD A (CI evidence) now seed-stable and P>=.978 -- the first CI movement all campaign; still not LB-testable (SE .006) and still NON-NESTED seq scores (nested check = 20 seq fits/seed, 2-4 h, not run); base CI reproduction .684-.690 sits below the published .7066 so quote the vs-published range.** Shipping recipe (~1.5-2 h): persist CI-head seq fold models (trunks already in seq_cache/trunk8), chunked seq cache + inference over ~800k eval hands / 6.9M candidate rows, add the 3 seq columns to evidence_v4's CI head (A_base|lambdarank|350), re-fit + re-Platt + eval, submit as a SINGLE-COMPONENT A/B (CI head only) vs the incumbent of sub010/sub011, and only if a slot + ~2 h exist before the D-1 freeze. Lead B (SP hand recall +.071, z 4.67) still unverified through the pair model. Doc: research/SEQMODEL.md 4.3/5 |
| exp033 | 2026-09-16 | **SP recall lead (exp032) followed END TO END into the pair model.** Round-3 hand detector rebuilt exactly as hs_v3 (SELFTRAIN, extras from hs_v2 = 2,097, w 1/25/8) PLUS the 4 seq margins (trunk8 fold models), applied only on soft-play gate G0 rows (22.5% of pair-hand rows; 100% of listed DT/SP, 93.5% CI); all other rows keep hs_v3 verbatim -> hs_seq/ (18M+12M rows, hs_v3 schema; re-scored dev rows = training OOF to 4e-8) -> handdet.agg with thresholds re-derived on the mixed OOF (handdet_models_seq/) -> incumbent pair command with PFH_TAG=seq. Controls: PFH_TAG=v3 reruns reproduce exp028_selfv3 and exp029_C101 BIT FOR BIT. Hand pre-check (listed-only): SP recall@q999 .421->.487 but @q99 .824->.798, SP AUC .99596->.99578. **C' exp033_seq .9604 vs exp028_selfv3 .9581 (+.0024, P(better) .755, 90% CI [-.0032,+.0074]); C2 .9657->.9676; SP .8789->.8857, DT .9551->.9550, CI .9727->.9726.** Seed-matched: C101 +.0033 (P .853, SP .8783->.8845), C202 +.0007 (P .583, SP flat); 3-seed bag vs exp029_bag .9578->.9599 (+.0022, P .738, SP .8802->.8851) | +.002 C' (consistent sign, 3/3 seeds) | not staged | **FAIL vs the bar (P>=.8 AND SP up): SP is up but P .755 (bag .738). A consistent, sub-threshold ~+.002 C'.** EVAL-SIDE RISK found: double 5-model averaging shrinks the eval score tail vs dev OOF (eval/dev rate above q999, sp: hs_v3 .834, hs_seq .651); SEQ_EVAL_SAMEFOLD=1 (score each eval table with its own frozen-fold model) restores it to .990 -> hs_seqsf/, oof/exp033_seqsf (same dev model, C' .9605). SIDE-FINDING: the INCUMBENT hs_v3 eval SP tail is itself shrunk to .83 of dev by the same averaging -> SP pairs may be slightly under-ranked in eval today; a same-fold hs_v3 eval rescoring (~10 min LightGBM) + pair refit would test it (not LB-resolvable). REPRO NOTE: handdet.agg is nondeterministic at 1e-8 (double-centring sums) -> +-.0001 C' between re-aggregations of identical hand scores. Reopen only with a variant that keeps the q999 sharpening without the q99 loss. Doc: research/SEQMODEL.md 7 |
| exp033 | 2026-09-16 | sequence-model features in the round-3 hand detector, re-scored on SP-gate rows, pair refit (agent): C' .9581→.9604, SP .8789→.8857, **P(better) .755**; seed 101 +.0033 (P .853), seed 202 +.0007 (P .583); bag +.0022 (P .738) | +.002 | — | FAILS the P≥.8 bar → not staged. Side-finding below is the valuable part |
| exp034 | 2026-09-16 | **TRAIN/SERVE MISMATCH FIX.** Dev hand scores are single-model OOF but eval hand scores were the MEAN of 5 fold models, which thins the tail: eval/dev rate above the q999 threshold = .881 DT / .880 SP / .876 CI / .779 any. The pair model's count/threshold features were learned on the sharper dev scores → SP (and others) likely under-ranked on eval, invisible to CV. Fix: score each eval table with the fold model that held that table out (SAMEFOLD_EVAL=1) → ratios 1.034 / 1.067 / .941 / .917. Dev C' unchanged by construction (.9576 both). Eval ranking: rank corr .985, top-300 overlap 289, top-500 493 | eval-only | sub013 pending | ADOPT as the default convention (principled; small expected LB effect, likely within noise). Round-2/3 detector models were overwritten by round 4, so the fix is applied to round 4 (CV-tied with round 3) |
| exp035 | 2026-09-16 | seed-bag (3x) of the same-fold round-4 pair model: C' .9574 vs exp029_bag .9578 (P .367, tied) | 0 on dev | sub013 staged | final-candidate A. sub010 (5-mean, round 3) is candidate B → the Sep 17 LB compares them |
| exp036pre | 2026-09-17 | quick listing-rule tests on dev (true-family routing): "earliest 5 detected planted hands" MAP .42-.49 (self-trained detectors WORSE: they also fire on unlisted planted hands); "type-1 planted then type-2, each chronological" .49-.51; by list length exp008 .795 vs detector-only .816 on the 32 short lists (not identifiable on eval). All far below exp008 .711 | — | — | simple rules REJECTED. Segment table confirms structure (DT seg-2 showdown .58 / pot 132bb vs seg-1 .13 / 23bb). Launched exp036: forensic search for the EXACT eligibility predicate ("first k hands with the behaviour-specific visible action") — the only lever with top-3 potential |
| exp036 | 2026-09-17 | **LISTING RULE FOUND (structure) + rule-guided evidence scorer.** Host list = sorted(EVENTS, key=(type, hand_seq))[:5]: ALL type-1 events of the phase in time order, then the EARLIEST type-2 events. Proof: seg-1 hands sit uniformly in the timeline (mean pos DT .498 / SP .499 / CI .411) while seg-2 hands are early (.248 / .309 / .172); unlisted T1-like hands in inversion pairs are at the base rate and flat before/after seg 1 (.0148/.0161 vs chrono-5 .0207/.0485) -> seg 1 is COMPLETE. **CI type is exact: # players who FOLDED before the pair's first preflop raise (0 = type 1: 405/405 chrono + 43/43 seg1; 1 = type 2: 12/12 seg2; 460/460 listed hands have <=1).** SP: type 1 = partner folds to partner aggression holding the better made hand (seg1 182/213 vs seg2 7/232), type 2 = passive call (223/232), 3 two-inversion lists add type 3 = strong check. DT: type 1 = donor fold-ahead (221/236 vs 32/213); identical action strings occur in both DT segments -> type only partly visible (type model OOF AUC .972 DT / .945 SP). **The EVENT (latent activation x visible action) is NOT a crisp predicate**: best deterministic rules exact-prefix CI .25 (MAP .736) / DT .15 / SP .03. Scorer: LightGBM event model on censoring labels (listed=1; unlisted in short lists or before the last-ranked listed hand=0; OOF AUC .991-.995) + type model + exact Poisson-binomial DP for P(listed) under the rule, blended 0.3 partner + 0.7 DP. **Dev OOF MAP@5 (true family) DT .7876 / SP .7543 / CI .7540 = .7675 vs exp008 .7164/.7078/.7066 = .7109 (+.057); DP alone .7615; vs exp027ev .7133 -> .7733 (+.060). Real posteriors (exp016/exp035): +.056..+.058 over exp008, +.059..+.060 over exp027ev, paired bootstrap P(better)=1.000.** Ablation: one-type DP .667/.718/.747 -> (type,time) DP .774/.756/.748 = the ordering is worth +.107 DT / +.038 SP | **+.057 dev evidence MAP ≈ +.011 final** | not submitted | ACCEPT as the evidence candidate. Files: scripts/listing_rule.py (diag/cv/eval; eval 136 s), research/forensics/listing_rule.md, data/derived/evidence_scores_{dev,eval}_exp036.parquet (partner exp008; eval 2.35M rows, 112,540 pairs, >=15 cands/pair) and evidence_scores_{dev,eval}_exp036_exp027ev.parquet (partner exp027ev); composition with sub011 risk passes validate_submission (0 non-eval, 0 non-shared); top-600 pairs share 4.28/5 hands with exp008. CAVEATS: under UNIFORM family posteriors the blend is worse (.611 vs .627) because every family's DP is peaked -> ambiguous/OC pairs may lose a little; dev file of exp008 is nested-optimistic so the eval gain may be larger. NEXT: ONE LB A/B = sub011 with evidence_scores_eval_exp036_exp027ev (pure evidence delta; expected +~.011, evidence SE ~.006); if it wins, it is the final evidence for both slots |
| exp036 VERIFIED | 2026-09-17 | Lead re-scored the agent's dev files with an independent evaluator: exp036_exp027ev true-family .7733 / **real posteriors .7710** vs exp027ev .7133 / .7115 (+.060); exp036 (exp008 blend) .7675 / .7651. Leakage audit of scripts/listing_rule.py: event & type models cross-fitted by frozen table folds (tr = fold != k); dp_scores uses only feature gates + OOF logits + constant cap 5; n_rel (true list length) appears only in the MAP evaluator and in TRAINING-fold labels → no validation leak. Eval posterior sharpness: top-300 pairs max-posterior ≥ .8 for 99.7-100% | +.060 dev MAP ≈ +.012 final | sub015-017 staged for 00:05 UTC Sep 18 | ACCEPT pending LB. THE LISTING RULE: all type-1 hands of the phase in time order, then the earliest type-2 hands, cut at 5 (CI type = exactly 0 vs 1 folds before the pair's first preflop raise; DT/SP type partly latent, learned AUC .972/.945) |
| sub015/016/017/018 | 2026-09-18 | **exp036 listing-rule evidence on LB**: sub015 (same risk as sub011, evidence only) .91773 vs .91117 ⇒ **+.00656 = +.0328 eval MAP (.666 → .699)**; sub016 (+ same-fold risk) **.91820 = best, 9th**; sub017 (exp008 partner) .91774; sub018 (PURE rule, w=1.0) .91549 ⇒ w=0.7 blend confirmed optimal (rule alone transfers worse). Dev gain was +.060 → only 55% transferred | +.0070 vs sub011 | best .91820 | Field moved: #1 .93827 #2 .93482 #3 .93468 #4 .93395 → gap to 3rd = .0165. Remaining loss: evidence .060 / pair .019 / behaviour .003 |
| exp038 | 2026-09-18 | pair-model rank fusions on C' (paired bootstrap 400): **f1 = same-fold bag + seq-feature model .9600 (+.0027, P .973)** · f2 = same-fold + round-3 bag .9582 (+.0008, P .850) · f3 = 3-way .9597 (+.0024, P .988) | +.0027 | not submitted (holding slots) | ACCEPT f1/f3: the seq-feature model FAILS alone (P .755) but adds in fusion — decorrelated errors. New best risk candidate |
| exp037 | 2026-09-17 | **TRANSFER GAP DIAGNOSED EXACTLY + a dev-neutral fix.** Fit obs_w = (1-phi)K_w + phi*alpha*L_w to the three matched-risk LB points (w=0 .6660 / w=.7 .6990 / w=1 .6854), K = dev MAP exposure-reweighted to the eval shared-hand distribution (.7137/.7740/.7678; eval exposure is NOT a loss -- reweighting RAISES dev), L = LOFO-with-the-rule values: **phi = .140, alpha = .90, RMSE .0006 (all three points reproduced)**. So known-family pairs transfer at ~100% and **~14% of true eval positives are unexplained by 3-family routing** (OC bound is 7-10% -> OC + mis-served variants); they score .377 under the partner alone but only .232 under exp036 -> 0.140*(.377-.232) = .020 of the .033 lost gain, the rest is the (1-phi) discount. **LOFO (rule scorer, held-out family): partner mixture .419 vs exp036 mixture .258 vs pure DP .203 vs pooled family-agnostic rule .260 (DT .336 / SP .385 / CI .059)** => the PRIORITY-2 OC-agnostic rule head is a NULL (worse than the incumbent mixture, same verdict as exp027ev's LOFO); the DP must instead be SWITCHED OFF where no modelled family fires. **exp037 = exp036 + family-strength gate**: strength = max_f top5-mean partner s_f, w_dp = 0 when strength < 0.6. Detector AUC for "family not modelled" .899 DT / .958 SP / .994 CI (the 3-class posterior is useless here: AUC .39-.59, 98% of the eval top-400 have maxP>.9). Dev: fires on 4.8% of positives, MAP .7710 -> .7719 (no cost). Eval incidence 6.0% top-200 / 8.5% top-400 / 13.3% top-600 (matches phi). Projected +.010..+.016 MAP = **+.002..+.003 final** | dev +.001 (neutral by construction), projected eval +.013 MAP | not submitted | SHIP-READY: evidence_scores_eval_exp037_exp027ev.parquet (2,271,506 rows, 112,540 pairs) + exp037 (partner exp008) + dev files; both pass validate_submission; top-600 lists share 4.88/5 with exp036; rerun via LR_TAG=exp037 LR_GATE_TH=0.6 scripts/listing_rule.py eval. **PRIORITY-3 NULLS (dev OOF, paired bootstrap): hs_v4sf detector scores in the event/type heads -.0127 overall, P(better) .002; one EM step with soft labels on the censored tail -.11 DP; cached seq margins DT -.013 / SP -.008 / CI DP +.020 but CI blend only +.004 (= +.001 overall, not worth 2-4 h of eval re-scoring).** The event head is already AUC .991-.995 vs rule-certain negatives; the missing bit is latent activation, which no available feature block carries. **CEILING NOTE: evidence caps at ~.715 with the current routing (known .774 + gate), so rank 3 (evidence ~.78) has to come from pairAP/behaviour, not evidence.** Doc: research/forensics/listing_rule.md 6 |
| exp037 | 2026-09-18 | evidence: switch the listing-rule DP OFF when no modelled family fires (strength = max_f top-5 mean partner s_f < 0.6 → keep the plain ranker). LOFO detector AUC .899/.958/.994; fires on 4.8% of dev positives at ZERO dev cost (.7710→.7719); eval incidence 6.0% of top-200, 8.5% top-400, 13.3% top-600 ≈ φ | projected +.010-.016 MAP = +.002-.003 final | in sub019 | ACCEPT. Mechanism: a family-specific DP is peaked on ~5 hands and picks confidently WRONG ones when the family is wrong (LOFO .258 vs .419 for the plain ranker) — so switch it off, never replace it with an agnostic version |
| exp039 | 2026-09-18 | LOFO of the PAIR model (train on 2 families, rank the held-out family's positives): AP_all .070 · AP_generic-only .0755 · 50/50 fusion .0791 (DT .113/.123/.132 · SP .056/.060/.061 · CI .042/.043/.045) | +.009 on a scenario that does not match reality | — | **REJECT the generic branch.** But the number is informative: if the unmodelled 14% were as alien as a held-out family, pair AP would be ≈.86, not the measured **.973** → those pairs ARE largely detected by the pair model (generic coordination signals suffice to DETECT; only the LISTING needs the family). Pair-AP headroom is genuinely small |
| audit_triads | 2026-09-18 | Three-player ring/triad collusion audit: checked all 53 open 2-hop paths (Y, Z) connected via multi-pair colluders X. 51/52 have dev OOF risk < 0.05 (mean 0.0012, 0 spies) | — | — | **REFUTED.** Triad/ring collusion is nonexistent in this generator; collusion is strictly pairwise. |
| audit_censor | 2026-09-18 | Censoring logic audit in listing_rule.py: tested max_seq vs last_by_rank (added 8,157 training negatives). Dev MAP dropped .7615 -> .7358 (-0.0257) | -0.0257 dev MAP | — | **CONFIRMED.** Hands occurring after segment 2 cutoff include truncated type-2 events; treating them as negative labels poisons the event model with false negatives. Claude's cutoff at the last segment-2 hand was correct. |
| exp040_lofo_beh | 2026-09-18 | LOFO simulation of predicting `other_coordination` when exp037 strength < 0.6: dropped macro-AP by -0.012 (DT held out), -0.001 (SP held out), -0.018 (CI held out), and -0.025 on dev positives | -0.012..-0.025 macro-AP | — | **REJECT.** Low strength is often due to low exposure or weak evidence on true known-family pairs. Setting their behavior to other_coordination turns them into false negatives with score 0, severely harming macro-AP. Keep argmax family for all pairs. |
| exp041_seedbag | 2026-09-18 | Seed bagging (1 vs 3 vs 5 seeds) on event/type models in listing rule: DP alone MAP .7615 -> .7618 -> .7623 (+.0008), blend with exp027ev .7733 -> .7712 (neutral/noise) | +.0008 DP | — | Bagging event/type models alone is variance-neutral; keep single model or full ensemble. |
| audit_sizing | 2026-09-18 | Bet sizing distribution and size->partner response dependence across 1.1M dev actions: partner response shifts match known soft play / flat-call traits; no separate sizing signalling channel | — | — | Sizing signalling hypothesis null; existing policy/surprise models already capture the action-level passivity. |
| exp042_ramp | 2026-09-18 | Evidence smooth ramp gate [0.45, 0.65]: w = 0.7 * clip((strength - 0.45)/0.20, 0, 1). Dev OOF MAP .7748 (highest overall, DT .7927 / SP .7660 / CI .7587). LOFO on unmodelled families recovers from .2832 to .3904 while avoiding the artificial knife-edge 0.60 cutoff on low-exposure eval pairs. Evals scored and written (evidence_scores_eval_exp042_ramp_exp027ev.parquet, 2.27M rows) | +.0015 dev MAP, projected +.004 LB | staged (sub022, sub023) | **ACCEPT as primary evidence candidate.** Assembled sub022_f1_exp042ramp (exp038_f1 risk + ramp evidence) and sub023_bagsf_exp042ramp (exp035_bagsf risk + ramp evidence). |
| exp044 | 2026-09-18 | GPU XGBoost pair risk model (tree_method='hist', depth 6) fused 50/50 rank-average with LightGBM GOSS. View C' jumps from .9574 to .9585; soft-play pair AP lifted .8767 -> .8817; P(better) .848. Superposed with exp042_ramp evidence in sub024 | +.0011 C', P .848 | **0.91954** (+.00109 vs sub023) | **ACCEPT as champion risk candidate.** Breaks 0.919 on LB. Tree-structure diversity works. |
| exp046 | 2026-09-18 | GPU CatBoost pair risk model (src/exp_cb_pairmodel.py, 400 iter, depth 6). Standalone raw View C AP reaches .7399 (highest single model). 3-way fusion (LGBM + XGB + CB) C' .9578 vs 2-way .9585 | — | — | CatBoost kept in reserve for possible evidence ranking / calibration. |
| exp048 | 2026-09-18 | Full 6-model seed bag: 3-seed LightGBM (exp035_bagsf) + 3-seed GPU XGBoost (exp047_xgbbags). View C' .9586, P(better) .858. Paired with exp042_ramp evidence in sub025 | +.0012 C', P .858 | **0.91942** (+.00097 vs sub023) | **ACCEPT as multi-seed champion.** Confirms XGBoost gain is stable and immune to single-seed variance. |

## 2026-09-20 — final push (7 lanes) and the last three submissions

| exp | date | CV | Δ | LB | verdict |
|---|---|---|---|---|---|
| **sub031 PROBE = sub025 with behaviour=other_coordination** | 2026-09-20 | — | — | **0.82325** | **DIRECT decomposition of the SHIPPED model.** = 0.7·pairAP + 0.2·evMAP + 0.1·b0 with evMAP = .70480 known exactly and b0 = .00175 ⇒ **pairAP .97445 · evidence .70480 · behaviour .96345**. The value inferred earlier from sub011's behaviour column was pairAP .97378 / behAP .96815 — off by +.0007 / −.0047, because **behaviour AP is risk-model dependent** (exp048's behaviour column differs from exp029_bag's in 531 rows and its top-600 family mix is CI 219 / DT 204 / SP 177 vs DT 228 / SP 187 / CI 185). Loss budget: pair .0179 / evidence .0590 / behaviour .0037. LESSON: probe the model you actually ship |
| exp068_famspecmix_w15 (sub028) | 2026-09-20 | 3 per-family specialist models (DT/SP/CI, `src/exp053_famspec.py`) rank-mixed into exp048 at w=.15. View C' .9586→.9614 (+.0028, **P 1.000**), C2 +.0022 (P 1.000), positive in 5/5 folds and all 3 families (DT +.0026 / SP +.0064 / CI +.0057), replicated at seed 43. BUT a new **view D** (the 137 frozen spies counted as POSITIVES rather than deleted) gives only +.0006 at P .728, and direct retrieval of those 137 from the unknown pool DROPS .9254→.9229 (w=.40: →.9177, view D +.0001 at P .544) | predicted +.0010 final | **0.91935 (Δ = −0.00007)** | **NULL on eval.** Pure risk A/B (evidence and behaviour columns bit-identical to sub025). The C'/C2 views DELETE the population where the cost lives, and every eval positive is by construction an *unlabelled* positive — i.e. a spy-like pair. Do not ship. Same failure mode as exp033_seqsf (dev C' +.0016 at P .990 → LB −.00063) |
| exp060_sp09 (sub030) | 2026-09-20 | soft-play event-head implied count × 0.90 (M3 side arm). Dev MAP@5 .7748→.7775 (**+.00271, P .887**, 4000-rep paired bootstrap); provably single-head — DT Δ = +.00000 (0/148 pairs moved), CI Δ = +.00000 (0/92), SP +.00764 (24 better / 15 worse / 93 tied); on eval the DT and CI score columns are bit-identical over 2.27M rows | predicted +.0005 final | **0.91905 (Δ = −0.00037)** | **FALSIFIED on eval** by its own pre-registered rule (Δ ≤ −.0003 ⇒ discard). Pure evidence A/B (risk column bit-identical). Implied ΔevMAP = −.00185 against a dev +.00271: transfer factor −.68 |
| lanes M1/M2/M4/B1/B3 | 2026-09-20 | counterfactual-value features in the event head; per-player×spot own baseline fit on partner-absent hands; exact expected-AP@5 ordering under the DP's joint law (vs marginal ranking); partner seed-bagging; a 5+5 seed bag | all below bar | — | **5 clean nulls.** The event bit remains the binding constraint and is not exposed by any feature block available here |
| exposure vs list length | 2026-09-20 | `scripts/exposure_listlength.py`: listed count rises with shared hands (Spearman +.246, p<1e-4; short lists median 80 shared vs 116; SP +.443, DT +.088, CI always 5) but strongly SUB-linearly (mean n_listed 4.723/4.926/4.914/4.978 across exposure quartiles). The DP's implied event count is LINEAR in hand count, and 26% of eval pairs sit below the minimum dev-positive exposure of 57 | — | — | Motivated exp060_sp09, which the LB then falsified. The sub-linearity is real; correcting for it is not worth score |

## 2026-09-20 — D-1 full re-audit (13 parallel agents). Everything below is measured; all model lanes null.

| exp | date | CV | Δ | LB | verdict |
|---|---|---|---|---|---|
| **BUGFIX: probe arithmetic** | 2026-09-20 | The sub014 probe (`predicted_behavior = other_coordination` everywhere) makes all three behaviour score vectors **all-zero**; `metric._average_precision` uses a *stable* argsort, so an all-zero vector is ranked in `pair_id` order and returns the AP of a RANDOM ranking, not 0. Measured E[AP] = **0.00466 +/- 0.00078** at public size (n 33,762, P~147), 0.00451 at private size (`scripts/phi_behap_bound.py`) | behAP .9664 -> **.97105**, pairAP .9733 -> **.97262** (exp029_bag) / **.97336** (exp048_fuse_bags); evidence unchanged | — | **CORRECTED.** The new triple reproduces sub025 (.91942) and sub026 (.91169) to 5 dp. Loss budget: pair .0186 / evidence .0590 / behaviour .0029 |
| **phi REFUTED (interpretation)** | 2026-09-20 | (a) `obs=(1-phi)K+phi*alpha*L` is linear in `((1-phi)c, phi*alpha*c)`: c=.92 -> phi=.065, c=1.00 -> .139, c=1.10 -> .218, identical RMSE — phi is NOT identified. With c=1 forced, phi = .14 +/- .04. A competing model `obs=c*K-g*w` (c=.933, g=.032) fits as well (RMSE .00074 vs .00057) with NO dead subpopulation. (b) **behaviour-probe bound**: other_coordination truth costs ~.95 of behaviour macro-AP per unit; behAP .9682 vs pairAP .9738 => **phi_OC <= .02** (simulation: phi_OC=.14 => behAP .84-.89). That leg binds only for HIGH-ranked OC; SECOND INDEPENDENT LEG for OC hiding LOW in the ranking: exp039's LOFO prices an unseen family's pairs at AP .070-.079, so `pairAP <= (1-phi) + .079*phi`, and pairAP .9738 forces **phi_undetected <= .0285**. Either way <= ~3%, not 14%. (c) the mis-served cohort profiles as low-exposure **soft play**; a 0/1488-calibrated statistic fires 183/174,000 dev vs 193/174,000 eval, **ratio 1.05** — no eval-only population. (d) the exp037/exp042 strength gate is an **exposure** gate: 0/25 of gate-firing top-200 eval pairs carry `fam_unexplained`, median shared hands 52 vs 76 | — | — | **The identified quantity is the transfer factor 0.86-0.93, not phi.** "Evidence caps at .715" is our own routing's value restated, not a property of the task. The 4th-family framing is removed from PLAN.md and the write-up |
| **ORACLE decomposition (evidence)** | 2026-09-20 | Same pipeline and folds as exp042_ramp (base .7748). Oracle TYPE .7930 (+.0182, P 1.000); oracle EVENT .9322 (**+.1574**; DT .793->.907, SP .766->.929, CI .759->.976); oracle BOTH .9420. Separately recall@5 .8199 / recall@10 **.9696** / recall@20 .9978 | — | — | **The listing problem is a ~10-candidate discrimination problem.** Perfect selection from our own top-10 = +.039 final; perfect re-ordering of our own top-5 = +.009 |
| leader arithmetic | 2026-09-20 | With our evidence .70485, #1's .93876 needs pairAP >= .9968 AND behAP = 1.0 -> impossible, so #1 beats us on EVIDENCE (feasible range [.73,.86], most plausibly .76-.80). Conversely a team with perfect known-family listing plus our other components would score .942-.950, above the actual best .93876 | — | — | **Nobody cracked the exact planting predicate.** Most plausible leader edge: a less family-brittle in-pair ranker |
| exp049_knobs (evidence) | 2026-09-20 | per-family blend w: 8^3 grid, best (.7,.6,.7) **+0.00098, P(better) .766**, SE .00139/cell; ramp bounds: dev min strength .4847 so **no dev pair sits below the low bound** — dev carries no information, best cell +.00059; LB quadratic w* = .6049 worth +.00017 final (3 points through 3 params = exact interpolation, zero residual dof); partner averaging (raw/rank/max/geometric of exp008+exp027ev): best STANDALONE ranker (+.0047) and **worst blend member** (-.0024 to -.0105, P <= .105) | all below the +.005 / P .8 bar | — | **ALL NULL.** Mechanism worth keeping: `s=(1-w)*partner + w*DP` needs the partner to be a **calibrated P(listed given family) on the DP's scale**; any re-ranking improves it as a ranker and degrades it as a blend member |
| exp050_event (evidence) | 2026-09-20 | player-specific normal-play baselines in the event head (shrunk per-(player,phase,bucket) policy recalibration): 32 feats **-.0096 (P .004)**, 6 feats -.0036 (P .104) — though the premise is real (per-player fold rates .42-.79 against a .61-.67 population band, 6.5x over-dispersion); per-pair q calibration already correct (sum P(listed) 4.89/4.76/4.93 vs true 4.90/4.79/5.00 on dev, 4.82/4.77 on eval top-200/400); within-pair lambdarank event head -.007 to -.032 (P .000); planted events are an exact **memoryless Bernoulli process** (first four listed hands at scaled positions [.195,.400,.587,.799] vs a Poisson null [.2,.4,.6,.8], spacing-variance ratio .97) so no temporal prior exists; the CI "trash raise" has **no hole-card range cut** (144/169 classes appear among listed hands; best threshold pf_eq <= .50 is a 2.1x enrichment, not a predicate) | all null | — | Sixth consecutive null on the event head. Sections 5.1 and 5.5 of listing_rule.md are now closed |
| exp051_typekey (evidence) | 2026-09-20 | Independent rebuild of the segment ground truth (DT 91 one-inversion lists = 449 hands, SP 93+3 = 460) plus a 231-column battery of purely mechanical action-log facts. The CI key reproduced EXACTLY and independently (43/43, 12/12, 405/405), validating the harness. Best mechanical DT rule .9109, SP .9239 — both below the .95 bar, and DT below the incumbent learned model's .927; an LGBM on the whole mechanical battery reaches .9198/.9022, i.e. strictly LESS type information than the 321-feature engine model. Falsified: generic fold-count unification, street index, showdown, action index, orientation bit, position | — | — | **NULL.** Two new exact *sufficient* conditions found (DT: donor folds facing the partner having never called them in that hand -> type 1, 64/64; an SP analogue) but neither moves the model |
| exp052_pairfuse | 2026-09-20 | CatBoost in the pair fusion at every weight: C' .9585 (w .05) -> .9577 (w .333); paired bootstrap w=.10 d -.00011 **P .410**, w=.20 d -.00048 P .293. L:X ratio flat .9583-.9586 over .3-.7; rank/logit/prob aggregation identical (.9586/.9586/.9587); a cross-fitted Caruana hill-climb is **worse** out-of-fold than fixed equal weights (.9635 vs .9639) with per-fold weights swinging .33-.87. Adding exp033_seqsf: C' +.0016 at P .990 — but the LB already falsified that direction (sub020 vs sub016: -.00063 pair AP against a +.0017 to +.0024 prediction) | — | — | **Pair lane closed.** exp048 vs exp044: d +.00006, P .543, rank corr .9951 — statistically identical, but exp044 embeds a SINGLE-seed XGBoost (seed spread .0028 in C'), so **exp048 is the lower-variance private-LB choice** |
| audit_repro | 2026-09-20 | `src/run_all.py` reproduced an early-September configuration (exp027_bag + exp008, ~LB .911) and was missing or mis-parameterised on 12 stages the shipped models need (eval equity replay, the no-policy engine pass, the audit_structure build, the exp004 tie-break score, oc_hunt events/stats plus all four export windows, self-training rounds 3-4 and the same-fold rescore, the v4sf mix tag, the whole evidence_v2 -> v4 -> listing_rule chain, the six seeded pair models, the four fusions). Fusion recipes re-derived and verified to rank corr **1.00000000**; exp048 is a **nested** 50/50 of two bags, not a flat 6-way mean | — | — | **FIXED.** New `src/run_all.py` (44 stages, `--list`/`--dry-run`), `README.md`, `requirements.txt`, an `XGB_DEVICE` env var, `.gitignore` `!` exceptions, frozen artefacts and the shipped CSVs committed, and `scripts/case_review2.py` (family-diverse, omniscient equities, per-hand benign alternative) -> `research/CASE_REVIEWS.md` |
| expM4_joint (evidence) | 2026-09-20 | **Exact E[AP@5] under the listing DP's JOINT law** (not marginals). New `scripts/lr_joint.py`: leave-one-out / leave-two-out Poisson-binomial gives u_i = E[rel_i/L] and v_ij = E[rel_i rel_j/L] exactly, so E[AP@5] = sum_k (1/k)(u_hk + sum_{j<k} v_hj,hk); exhaustive search over ordered 5-subsets of the top-10. Validated to 1e-16 against brute-force enumeration (marginals == listed_prob, u/v == the true expectations, best_order == the exhaustive E[AP] maximiser). Baselines reproduce the shipped numbers EXACTLY (DP-only .7615, exp042_ramp .7748 = DT .7927 / SP .7660 / CI .7587). Results: pure DP .7615 -> .7618 (P .584); blend .7748 -> .7743 (P .276); partner(x)DP two-component mixture .7748 -> .7754 (P .747); family mixture at real posteriors .7735 -> .7735 (P .520). **The model's OWN E[AP] gain from the optimal reordering is +0.00004 to +0.00008** - marginal ranking is optimal to within 1e-4, invariant to candidate set (10/15/20 identical) and to conditioning on L >= 3. Mechanism: the DP's dependence is strong (implied corr(rel_i, rel_j) +0.45 mean) but NESTED - listed sets are prefixes of the (type, time) order - and marginal ranking is optimal for AP under a nested law. The L channel is not the problem either: the DP's E[L] is well calibrated (corr with true n_rel +0.730 vs +0.204 for raw exposure; Q1-exposure true 4.714 vs model 4.711). The one regime where the joint objective DOES pay is a genuinely multimodal law: tempering the family posterior gives +.0003 at max-P .9, +.0006 at .8, **+.0065 at .5 (P .927) and +.0131 at 1/3 (P .959)**, with the realised gain matching the predicted gain (+.0123) to 6%. On eval the behaviour posterior is one-hot exactly where the positives are (top-200/400 risk: max-P mean .998/.995, 0.0% below .8), so there is no population to collect it from | +0.00004 model-optimal (measured -.0005..+.0006, all P < .8) | not submitted | **NULL - marginal ranking is E[AP@5]-optimal to within 1e-4 under the DP's own law.** Closes the 'ranking by marginals is only optimal under independence' objection with an exact computation instead of an argument. REOPEN if a listing model carries a latent pair-level mixture (two competing 'which events fired' hypotheses), or if evidence is ever routed through a flat family posterior (max-P <= 0.6): `scripts/lr_joint.py` is built, exactly validated and delivers +.013 there. Repro: `python scripts/lr_joint_fit.py`, then `python scripts/lr_joint_dev.py --cand 10 --mode both`, `lr_joint_mix.py`, `lr_joint_pdmix.py`, `lr_joint_check.py` |
| exp060_M3exposure (evidence) | 2026-09-20 | **Lane M3 — "the DP's event count is mis-extrapolated below the dev exposure range" — REFUTED on three independent tests.** (1) *The physical law is real*: censored-Poisson fit of `n_listed = min(N,5)`, `log lam = a + b·log n_shared + family`, on the 372 dev positives (only 32 uncensored lists: 11 at 3, 21 at 4) gives **b = 0.517, profile 95% CI [0.28, 0.78]**, LR(b=1) = 11.9 (p = .00056), LR(b=0) = 18.9; per family DT 0.18 [-.12,.50], SP 1.04 [.60,1.55], CI unidentified (0/92 short lists). lam(n=112) = 8.8 (DT) / 7.8 (SP). (2) *But the DP does not extrapolate linearly*: the event model's implied count S = sum q is **already sub-linear inside support** (log-log slope DT +.08 / SP +.47 / CI +.30) and — the decisive test — refitting the event+type heads on dev pairs with `n_shared >= 86` (and again `>= 112`) and applying them to the held-out low-exposure pairs gives a **crossing slope of +.20/+.57/+.16 (T=86) and +.20/+.51/+.25 (T=112), i.e. the same as in support, not the +1.0 the premise requires. There is no kink at the support boundary.** (3) *Every correction loses*: forcing S := lam_hat(n) per pair (single multiplicative logit shift, bisection) costs **-.0088 overall (P(better) .000)** and **-.0153 on the bottom exposure quartile**; the task's own held-out protocol (law fitted on the top 3 quartiles, applied to the bottom) gives **-.0080, P(better) .067**; in the simulated-boundary test every arm loses on the genuinely out-of-support pairs (model-slope de-kink -.0145/-.0195, physical-law b -.0126/-.0090, flat x1.15 -.0111/-.0026, flat x1.35 -.0616/-.0457). (4) *The premise's headline number was a routing artefact*: the "implied list length 2.9-3.5 below support" reads the DP of all three families for the same pairs and includes low-risk pairs; the **posterior-routed** `sum_f P_f·L_f` for the top-300 eval pairs is **4.42-4.99 in every exposure band incl. 38-49 shared hands** (dev: 4.71-4.94). (5) *Why any rescaling is a bad bet*: S is an effective parameter at a **sharp MAP optimum** — a global x0.85 costs -.0138, x1.15 costs -.0302, x1.35 -.0729; the fold-honest optimum is exactly **1.0 in 5/5 folds** | law b<1 confirmed; all corrections negative | — | **NULL — lane closed.** Harness: `scripts/lane_m3_exposure.py` (censored-count fit, numba per-pair logit-shift solver, paired bootstrap), `scripts/lane_m3_cache.py`, `scripts/lane_m3_kink.py`. Reopens only if a *measured* eval-side calibration (not a fitted law) shows the routed implied list length below 4 on high-risk low-exposure pairs |
| exp060_sp09 (evidence, SIDE RESULT) | 2026-09-20 | Falling out of the M3 sweep: the fold-honest optimum of the implied event count is 1.0 for DT and CI but **0.90 for SOFT PLAY in 5/5 folds** (SP dev MAP .7660 -> .7736; the hump is broad, .85/.90/.95 all >= incumbent, so the exact 0.90 is not identified). One multiplicative logit shift per pair on the SP head only, everything else identical to exp042_ramp. Dev OOF MAP@5: true-family routing **.7748 -> .7775 (+.0027, P(better) .882)**; real posteriors exp048_fuse_bags **.7484 -> .7523 (+.0039, P .974)**, exp035_sf101 .7735 -> .7762 (+.0027, P .882). Coherent with SP having the noisiest type key (AUC .945 vs DT .972), i.e. less trust in the (type,time) ordering | +.0027..+.0039 dev MAP, P .88/.97 | not submitted | **Clears the project's evidence bar (P >= .8) but is worth only +0.0005 +/- 0.0002 of final score** — below LB noise (+/-.006). Files: `data/derived/evidence_scores_{dev,eval}_exp060_sp09_exp027ev.parquet`; build with `python src/assemble.py <sub_id> exp048_fuse_bags scores:data/derived/evidence_scores_eval_exp060_sp09_exp027ev.parquet`. Take it only if a slot is being spent anyway; it is a one-component change vs sub025 |
| exp062_pllr (evidence) | 2026-09-20 | **Lane M2 — the per-player normal-play baseline rebuilt at ACTION granularity, so it CAN reorder hands within a pair — MEASURED NEGATIVE.** `src/playerllr.py`: per (player, phase) ridge-logistic OFFSET over the population policy (`gamma = 0` reproduces it exactly), 3 binary sub-decisions (fold-vs-continue facing a bet; agg-vs-call given no fold; agg-vs-check with no bet), d = 6 state features (own perceived strength, its square, postflop, strength x postflop, pot odds), lambda = 25 chosen on held-out likelihood. **Contamination guard = PHASE-LOCALITY, not partner exclusion**: colluder sets are disjoint across phases, so dev-phase hands are scored with an eval-phase-fitted baseline and vice versa (the partner-exclusion the task proposed needs ~225k refits on eval and is strictly weaker). *Premise CONFIRMED on three checks*: (1) per-player style is real and persistent — **cross-phase held-out log-likelihood gain +0.0426 / +0.0443 nats per decision** vs the population policy (in-sample +0.057, so 78% transfers); (2) the resulting per-hand surprise separates listed evidence hands from unlisted shared hands at **AUC 0.902** (DT .917 / SP .909 / CI .869) standalone; (3) **98.4% of its variance is WITHIN pairs** — exactly the property exp050's 8-bucket feature lacked. *Outcome NEGATIVE anyway*: dev OOF MAP@5 (true-family routing, ramped blend, paired on the frozen table folds) **.7748 -> .7676 with 9 features (-.0072, P(better) .008 pair / .005 table)** and **.7647 with 26 features (-.0101, P .000 / .002)**; real posteriors (exp035_sf101) .7735 -> .7668 (-.0067, P .017). *Mechanism, both repair routes closed*: (a) the event head's own OOF AUC vs the censoring-derived negatives moves by **+0.0002** (DT .9944->.9944, SP .9945->.9948, CI .9914->.9911) — the signal is **fully redundant** with the 321 engine features (spearman .32-.40 with `surp_face_*`), so there is no new event bit; (b) it is **not a calibration shift** — the DP's implied list length is unchanged at 4.897/4.771/4.922 vs base 4.893/4.764/4.931 against true 4.899/4.788/5.000, so an exp060-style logit rescale cannot recover it. The features merely move the same total mass onto the wrong hands | -.0072 to -.0101 dev MAP, P <= .017 | not staged | **NULL — seventh consecutive null on the event head, and the third independent refutation of the player-baseline premise (exp050 -.0096 / this -.0072).** The right diagnosis of exp050 ("8 buckets cannot reorder within a pair") was acted on in full and the conclusion did not change: **the latent EVENT bit is not a function of how unusual the action is for the player.** Artefacts: `src/playerllr.py`, `data/derived/player_llr_hand.parquet` (5,003,979 (hand, player) rows), `data/derived/player_llr_gamma.npz`, `scripts/listing_rule_m2.py` (frozen copy of the runner), the `LR_LLR` block in `scripts/listing_rule.py` (default off — the base arm reproduced the incumbent .7733 / .7748 exactly). Repro: `LR_RES=listing_rule_m2.json LR_LLR=small LR_TAG=m2small LR_GATE_RAMP=0.45,0.65 LR_PARTNER=exp027ev python scripts/listing_rule_m2.py cv` |
| exp065_evbag (evidence) | 2026-09-20 | **Seed-bag of the exp027ev in-pair partner ranker** — 3 independent full reseeds (pooled stage-1 + segment model + stage-2 chronology ranker, each with its own Platt), `src/exp050_bag027ev.py`. This is NOT the closed "partner averaging" lane (that mixed exp008+exp027ev, two architectures on different calibration scales); seed-bagging one architecture is pure variance reduction, and the three aggregations include `replatt` = CLOSED_LANES' own reopen condition ("a partner that is re-calibrated after averaging"). **Standalone the bag works**: 0.71433 vs single seeds 0.70789/0.70517/0.71118, **+0.00644/+0.00916/+0.00315, P(better) .977/.997/.805**, +0.00625 over the mean seed. **In the exp042_ramp blend it vanishes**: 0.77075 vs 0.77088/0.77206/0.77175, **−0.00013/−0.00132/−0.00100, P .471/.211/.292** (Ptab .471/.228/.302, boot sd .0018); all three aggregations agree (probmean .77075, margmean .77091, replatt .77072). Robust over the whole flat-w grid: the bag-minus-mean-seed delta decays +.00625 (w 0) → +.00210 (.3) → −.00083 (.5) → −.00128 (.7) → 0 (w 1). **Mechanism**: the whole partner is worth only +0.01172 of blend MAP over the pure DP at w=0.7 (.77325 vs .76153), and bagging lifts the partner by 0.9% of its own level, so the transfer is ≪ the .0018 bootstrap sd. It is NOT the calibration-scale story — the partner's within-pair top-5 sd is unchanged by averaging (dt .1635/.1628/.1679 → .1650) and re-Platting moves the blend by 0.00003 | standalone +0.00625 P .977–.997 · **in-blend −0.00081, P .21–.47** | not submitted | **NULL — below bar (+0.003 at P ≥ .8). Do not spend a slot.** Seed-to-seed spread of the shipped evidence column is small in-blend (.00118 over 3 seeds ≈ ±.0002 final), so 0.7048 is not single-seed luck. Reopen only if the partner's blend weight rises above ~0.5 (1−w > 0.5) |
| **PROVENANCE DEFECT (evidence)** | 2026-09-20 | The shipped `evidence_scores_{dev,eval}_exp027ev.parquet` — the partner behind sub024/sub025/sub027 — were built by `evidence_v4.run_eval` with **`s1_mode="outer"`** (stage-2 trained AND served on the cached 80% outer-OOF stage-1), a branch **unreachable from the CLI**: `__main__` and `src/run_all.py` both call `run_eval()`, whose default is `s1_mode="inner"`. An "outer" rebuild reproduces the shipped dev file **bit-for-bit** (max abs diff 0.000e+00 over all 45,129 rows × 3 families; scratch `provenance.py`); the "inner" rebuild is a *different model* (dev OOF dt .7236/sp .7072/ci .6837 vs shipped .7222/.7191/.6907) worth **−0.0039 dev MAP inside the exp042_ramp blend** (.77088 vs .77480). The two historical logs match exactly: `logs/exp027ev_eval3.log` = inner, `logs/exp027ev_eval_final.log` = outer (and outer is 2.5x faster per family because it skips the inner_s1 loop) | — | — | **`src/run_all.py` does not rebuild the submitted evidence column.** Fixed additively: `evidence_v4.run_eval` now reads `EVID_S1MODE` (default unchanged at "inner"). **ACTION for the prize deliverable**: add `"EVID_S1MODE": "outer"` to the `evidence_v4` stage env in `src/run_all.py` (line ~202). Also note the shipped dev MAP .7748 is measured train-sharp/serve-sharp, i.e. mildly optimistic; paired deltas are unaffected |
| exp053/054/055 (pair, lane B3) | 2026-09-20 | **DEFECT FOUND in the champion's bagging provenance, then priced at ~0.** Both pair models bag their FINAL eval prediction over `seed = 42+i, i<3` **hardcoded**; the `SEED` env var reaches only the 5 OOF fold models. So on the side that actually ships, the three "seeds" of the LightGBM bag are **bit-identical** (`exp034_v4sf` == `exp035_sf101` == `exp035_sf202`, 0 ulp; `rank corr(exp035_bagsf, exp034_v4sf) = 1.00000000` -- the bag adds literally nothing), and the three XGBoost "seeds" differ **only by CUDA-hist atomics nondeterminism** (rank corr .9897-.9939). True eval-side bag widths are LGB **3 fits** / XGB **9 fits**, not "3 seeds + 3 seeds". Measured for the first time, genuine seed diversity is much larger than that same-seed noise: 9 distinct-seed full-data fits have mean pairwise rank corr **.9503** (XGB) and **.9797** (LGB), vs .9897-.9939 for XGB same-seed re-runs. Consequence for the endgame note in exp052: exp044 and exp048 differ on the eval side ONLY in XGB bag width (3 vs 9 fits) -- their LGB halves are the same bytes -- and their **top-500 eval sets are 100% identical** (top-100 98%, top-1000 99.5%), so the sub024/sub025 LB gap of .00012 is noise and the quoted ".0028 single-seed spread" is an OOF spread that never described the shipped file. **Variance budget** (dev view C', measured): sd(single fit) LGB .0006 / XGB .0014; sd(nested 1+1 over 9 seed pairs) .0005; sd scales as 1/sqrt(k) (XGB k=1 .0014 -> k=2 .0007). Hence sd(C') = **.00024** for shipped exp048 (3+9), .00029 for exp044 (3+3), **.00017** for a wide 9+9 -- a final-score sd of .00014-.00016 falling to .00010-.00011. Label-free convergence agrees: top-500 of a 1-fit bag already overlaps the 9-fit bag 494/500, a 3-fit bag 498/500 | E[delta] **+0.0001** (Jensen gain, extrapolated from the measured k=1 -> k=3 mean rise of +.0003 LGB / +.0006 XGB); sd reduction **-0.00005** | not submitted | **NULL vs the slot question -- do NOT spend a submission on it.** Two independent reasons: (1) the expected gain and the variance saved are both ~1e-4, i.e. **40-100x below LB noise (+/-.006)** and ~180x below the +.01829 gap to 3rd; (2) the change is **eval-side only, so the project's own decision bar cannot see it** -- `eval_views` gives C' .9586 / C2 .9661 for exp055_fuse_bags9 and exp048_fuse_bags alike (the OOF is unchanged by construction) and `scripts/boot.py` returns **delta exactly +0.0000** on all 400 replicates. Artefacts are built and are strictly-better estimators of the same target if a slot is being spent anyway: `oof/exp053_bagsf9_*`, `oof/exp054_xgbag9_*` (each = 9 TRUE seeds 42-50) and `oof/exp055_fuse_bags9_*` (nested 50/50; rank corr .99682 vs exp048, top-500 overlap 498/500). Also NULL: the rank-average fusion puts **23.7% of eval rows in tie-groups** (max group 6; 243 of the top-1000), but the tie-break is worth nothing -- C' .95856 with a stable argsort vs .95857 for the best of {exp004 s_z, mean raw prob, finer 6-way rank}, and the sd across 30 RANDOM tie-breaks is **.00001**. **NEW REPRO CAVEAT (second one, alongside handdet.agg):** LightGBM here is bit-identical *within* a process/machine state but **not across runs** -- a faithful re-fit of seeds 42/43/44 lands at rank corr **.99570** against the shipped `exp034_v4sf`, well above the independent-3-bag baseline of .9930 (so the seeds do match) but not at 1.0; likely the load-dependent row-wise/col-wise auto-choice. `src/run_all.py` therefore reproduces the LightGBM pair models to ~.996 rank corr, not bit-exactly; the gap is inside seed noise and does not threaten the score. Infrastructure added: `EVAL_SEED_BASE`/`EVAL_SEEDS` env vars on both pair models (defaults 42/3 are the literal previous constants, so the code path is unchanged), `gc` frees in the XGB fold loop (numerically inert; fixes "bad allocation" on a contended host), `scripts/eval_seed_fits.py` (final-fit-only, ~5x cheaper, no X[fold] copy), `scripts/build_wide_bags.py` |
| exp061/062 (pair, lane B2) | 2026-09-20 | **PER-FAMILY SPECIALIST MIXTURE — ACCEPTED.** Diagnosis first: view-C' soft-play AP .8817 (vs DT .9602 / CI .9660) is carried by **15 of 117 SP positives** with >20 negatives above them; those 15 have median shared-hand count 57 (vs 90) and 1-4 listed planted hands inside the w2000a window (vs 5). The hand detector's score on planted hands is **exposure-independent** (spearman(n, median planted s_sp) = .06), so this is NOT detector miscalibration — their planted hands are intrinsically weak (median s_sp .13 vs .64) and they are below the well-ranked SP positives on *every* SP signature, so no hidden SP variant exists. Oracle: rescuing the 5 mid-band pairs = +.0026 pooled C', all 15 = +.0058. Also measured: view C' holds positives with 0-2 in-window listed hands; restricted to >=4, **DT goes .9602 -> .9938 while SP only goes .8817 -> .9088** (the worst DT positive, 17,052 negatives above it, has ZERO listed hands in the window) — DT is solved, SP is genuinely the hard family. **Intervention** (`src/exp053_famspec.py`): three family-restricted pair models, identical features/folds/params to the champion, each trained on ONE family's positives vs the labelled negatives + unknowns (other families' positives and the frozen spy pairs dropped). Each beats the pooled champion on its own family: DT **.9681** (+.008), SP **.9206** (+.039), CI **.9866** (+.021). Combination: `s = .6*rank(exp048_fuse_bags) + .4*rank(p_DT + p_SP + p_CI)` (`scripts/mixmax.py rmix`) | **C' .9586 -> .9630 (+.0044), P(better) 1.000, 90% CI [+.0020,+.0072]; C2 .9661 -> .9693 (+.0032), P .999.** Every family up: DT +.0029, SP +.0109, CI +.0106. Flat in w over .25-.60 | — (not submitted; orchestrator owns the slots) | **ACCEPT.** Negative control rules out "one more model": the same script with the POOLED objective and the same spy hygiene (`exp061_poolnospy`) blended at the same w=.40 gives **+.0000, P(better) .484** (C2 -.0004, P .324), and the specialist mixture beats that control by +.0045 at P .999 — the gain is the *specialisation*, not the extra member. Shipped as `oof/exp062_famspecmix_{oof_w2a,eval_scores}.parquet`; evidence and behaviour columns are byte-identical to sub025, so it is a clean one-component risk A/B. **NULLS in the same lane:** count-aware co-player block `ob2_*` (binomial-z, gap-to-best-rival, softmax share, for all 11 sp_* flags; `src/pairfeat_ob2.py`) — SP AP .8701 -> .8598 in a matched probe, do not reopen; linear rank-blend of the SP-only specialist into the pooled risk — catastrophic at **any** weight (DT .9602 -> .8878 at w=.05), because the specialist ranks DT/CI positives near the bottom; `max(qmap(base), lam*p_SP)` — +.0012 C' at P .932 but with a **cliff between lam 1.0 and 1.1** (pooled .9599 -> .9468 as one DT positive is overtaken), rejected as unshippable |

## 2026-09-21 — FINAL RESULT

| exp | date | CV | Δ | LB | verdict |
|---|---|---|---|---|---|
| **FINAL** | 2026-09-21 | — | — | **private 0.92817, 8th of ~370** | Ticked finals: sub025 (private .92777) and **sub028 (private .92817)**. The hedge won by **+0.00040** — the E[max] argument held. Best private across all 29 submissions was sub020 at .92822, only +0.00005 above what we ticked, so selection cost ~nothing. Public 11th/.91954 → private 8th/.92817; mean public→private shift over our submissions +0.0126 (sd 0.0071). Prizes: Tejasv .94320 / blastyy .94263 / John Tyler .94196; **gap to 3rd +0.01379**. PRIVATE components (probes sub011/012/014/026/031): pair AP **.9807** · evidence **.7218** · behaviour **.9692**. Solving for 3rd: even perfect pair AP AND perfect behaviour still needs evidence ≈ .710, so they beat us on BOTH — evidence ≈ .75–.77 and pair AP ≈ .985–.99. NOTE: sub020 (seq-fusion risk, rejected on a −0.00044 public delta) had the best private score of anything we built — a −0.0004 public reading is absence of evidence, not falsification |
